add_newline = false
format = "[┌──────────────────────────────>](bold green)\n[│](bold green) $env_var$all\n[└─>](bold green) "
[env_var.NGROK_ENV]
format = "NGROK_ENV=[$env_value]($style) "
style = "bold green"

[character]
error_symbol = "[✗](bold red) "
success_symbol = "[➜](bold green) "

[cmd_duration]
style = "blue"

[aws]
disabled = true

[nix_shell]
disabled = true

[buf]
disabled = true

[golang]
disabled = true

[direnv]
disabled = false

[rust]
disabled = true

[package]
disabled = true

[nodejs]
disabled = true

[localip]
disabled = true

[line_break]
disabled = true

[docker_context]
disabled = true

[hostname]
format = "[codespace]($style) "

# custom module for jj status
[custom.jj]
ignore_timeout = true
description = "The current jj status"
detect_folders = [".jj"]
symbol = "🥋 "
command = '''
jj log --revisions @ --no-graph --ignore-working-copy --color always --limit 1 --template '
  separate(" ",
    change_id.shortest(4),
    bookmarks,
    "|",
    concat(
      if(conflict, "💥"),
      if(divergent, "🚧"),
      if(hidden, "👻"),
      if(immutable, "🔒"),
    ),
    raw_escape_sequence("\x1b[1;32m") ++ if(empty, "(empty)"),
    raw_escape_sequence("\x1b[1;32m") ++ coalesce(
      truncate_end(29, description.first_line(), "…"),
      "(no description set)",
    ) ++ raw_escape_sequence("\x1b[0m"),
  )
'
'''

# optionally disable git modules
[git_status]
disabled = true

[git_state]
disabled = true

[git_commit]
disabled = true

[git_metrics]
disabled = true

[git_branch]
disabled = true

# re-enable git_branch as long as we're not in a jj repo
[custom.git_branch]
when = true
command = "jj root >/dev/null 2>&1 || starship module git_branch"
description = "Only show git_branch if we're not in a jj repo"
